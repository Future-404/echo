# Echo 部署配置
# 複製此文件為 .env 並修改

# ── Docker / Node 部署 ──────────────────────────────────────────────────────
# 後端認證 Token（強烈建議修改）
# 生成方法: openssl rand -hex 32
AUTH_TOKEN=please_change_this_token

# ── Cloudflare Pages 部署 ───────────────────────────────────────────────────
# 前端 build 時設置，指向 Worker URL
# VITE_API_URL=https://echo-storage.your-subdomain.workers.dev
