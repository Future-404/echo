services:
  echo-dev:
    image: node:24-alpine
    container_name: echo-dev-env
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "8888:8888"
    command: sh -c "npm install && npm run dev"
